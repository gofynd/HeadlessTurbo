# Non-secret deployment defaults.
# DO NOT add APPLICATION_ID or APPLICATION_TOKEN here — those are secrets and
# must be injected by the deployment platform (boltic secrets / runtime env vars).
PROXY_TARGET=https://api.fynd.com
DOMAIN=api.fynd.com
PORT=8080
APPLICATION_ID=668765e1c984016d78222a21
APPLICATION_TOKEN=mipsexUEX
